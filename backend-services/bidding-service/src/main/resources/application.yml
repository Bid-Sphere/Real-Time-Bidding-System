spring:
  application:
    name: bidding-service
  datasource:
    url: jdbc:postgresql://localhost:5434/bidding_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 8083

services:
  auth-service:
    url: http://localhost:8081
  auction-service:
    url: http://localhost:8082
