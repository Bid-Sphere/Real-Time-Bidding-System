spring:
  application:
    name: project-auction-service
  datasource:
    url: jdbc:postgresql://localhost:5433/project_auction_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 8082

services:
  auth-service:
    url: http://localhost:8081
